sprite_gen_done=slime-coinpile-ai
folder=C:\dev\game2\assets\generated\sprites\slime-coinpile-ai
engine=component-row
files=sprite-request,raw,frames,atlas,manifest
qa_note=pass: three bottom-anchored coin pile stages generated at 256px cells.

## State Verdicts

- pile1: pass, small coin stack reads as the first wealth stage.
- pile2: pass, medium coin mound reads as the second wealth stage.
- pile3: pass, overflowing treasure pile reads as the final wealth stage.
