# slime-challenges-ai QA Notes

- Runtime atlas contains 4 generated challenge icons: silence, mono, starlit, sprint.
- All states are static 1-frame 256x256 icons using green chroma (`#00FF00`) because the set includes pink/lavender/violet subjects.
- QA verdict: pass. `silence` reads as no-click hand, `mono` as one-path ranch, `starlit` as shooting-star hunt, and `sprint` as stopwatch/speed challenge at shop icon scale.
- Sheet size: 256 x 1024. Rows: `silence` y=0, `mono` y=256, `starlit` y=512, `sprint` y=768.
